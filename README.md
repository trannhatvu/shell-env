# Shell Env

## Install 

```bash
cd ~
git clone https://github.com/trannhatvu/shell-env.git
```
Add the following line to your `~/.zshrc` or `~/.bashrc` file:

```bash
if [ -f ~/shell-env/main.sh ]; then
  source ~/shell-env/main.sh
fi
```